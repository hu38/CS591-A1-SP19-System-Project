#include "LSM.h"
using namespace std;

void LSM::print_LSM() {
    cout << "LSM data" << endl;
    return;
}

string LSM::get_value(int key) {
    // if Buffer.getData(key):
    //     return Buffer.getData(key)
    // else:
    //      return Level.getData(key)
    return("");
}